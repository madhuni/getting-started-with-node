/**
 * This enum contains the contants for the Server
 * `PORT` = `1400` // value of the port for server to run
 * `HOST_NAME` = `"localhost"`
 *
 * @export
 * @enum {number | string}
 */
export const enum ServerConfig {
  PORT = 1400,
  HOST_NAME = "localhost"
}
